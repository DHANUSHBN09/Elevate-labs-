# Data Analyst Internship - Task 1

## 🧹 Task: Data Cleaning and Preprocessing
This project focuses on cleaning and preparing raw datasets with missing values, duplicates, and inconsistent formats.

### 🧰 Tools Used
- Python
- Pandas Library

### 🧩 Steps Performed
1. Loaded raw dataset
2. Handled missing values using `fillna()` and `dropna()`
3. Removed duplicate rows using `drop_duplicates()`
4. Standardized text values (gender, country, etc.)
5. Fixed inconsistent date formats
6. Renamed column headers to lowercase and underscore format
7. Checked and corrected data types

### 📁 Deliverables
- `cleaned_data.csv` — final cleaned dataset
- `data_cleaning.py` — cleaning script
